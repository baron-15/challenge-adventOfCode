count = Array.new(2,0)
File.open("input.txt", "r").each { |line| count[0] += 1 if (((((line.partition(',')[0][0..line.partition(',')[0].index('-')].to_i) .. (line.partition(',')[0][line.partition(',')[0].index('-')+1..-1].to_i)).to_a - ((line.partition(',')[2][0..line.partition(',')[2].index('-')].to_i) .. (line.partition(',')[2][line.partition(',')[2].index('-')+1..-1].to_i)).to_a).length == ((line.partition(',')[0][0..line.partition(',')[0].index('-')].to_i) .. (line.partition(',')[0][line.partition(',')[0].index('-')+1..-1].to_i)).to_a.length - ((line.partition(',')[2][0..line.partition(',')[2].index('-')].to_i) .. (line.partition(',')[2][line.partition(',')[2].index('-')+1..-1].to_i)).to_a.length)||((((line.partition(',')[2][0..line.partition(',')[2].index('-')].to_i) .. (line.partition(',')[2][line.partition(',')[2].index('-')+1..-1].to_i)).to_a - ((line.partition(',')[0][0..line.partition(',')[0].index('-')].to_i) .. (line.partition(',')[0][line.partition(',')[0].index('-')+1..-1].to_i)).to_a).length == ((line.partition(',')[2][0..line.partition(',')[2].index('-')].to_i) .. (line.partition(',')[2][line.partition(',')[2].index('-')+1..-1].to_i)).to_a.length - ((line.partition(',')[0][0..line.partition(',')[0].index('-')].to_i) .. (line.partition(',')[0][line.partition(',')[0].index('-')+1..-1].to_i)).to_a.length))}
File.open("input.txt", "r").each { |line| count[1] += 1 if (((((line.partition(',')[0][0..line.partition(',')[0].index('-')].to_i) .. (line.partition(',')[0][line.partition(',')[0].index('-')+1..-1].to_i)).to_a - ((line.partition(',')[2][0..line.partition(',')[2].index('-')].to_i) .. (line.partition(',')[2][line.partition(',')[2].index('-')+1..-1].to_i)).to_a).length != ((line.partition(',')[0][0..line.partition(',')[0].index('-')].to_i) .. (line.partition(',')[0][line.partition(',')[0].index('-')+1..-1].to_i)).to_a.length))}
puts "Answers are #{count.to_s}"